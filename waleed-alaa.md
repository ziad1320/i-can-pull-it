```cpp
#include<iostream>
#include<math.h>
#include<iomanip>
#include<string>
#include<algorithm>
using namespace std;

int main() {
    long long num;
    cin >> num;
    int ans = (num * (num + 1)) / 2;
    cout << ans << endl;
    return 0;
}
```
